yolo_model:

  config_file:
    name: simulated_test_3l_large.cfg
  weight_file:
    name: simulated_test_3l_large_best.weights
  threshold:
    value: 0.2 # Leave small b/c the yolo_processor will handle thresholding
  detection_classes:
    names:
      - Cutie
      - Crucifix
      - Garlic

image_view:
  display_detected_image: false
